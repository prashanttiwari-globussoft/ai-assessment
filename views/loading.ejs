<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Processing Resume</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="container loading-container">
        <div class="loading-spinner"></div>
        <h2>Generating Questions...</h2>
        <p>We're generating customized questions based on your experience and skills.</p>
        <p>This may take a moment.</p>
        
        <script>
            // Redirect to first question when processing is complete
            const sessionId = '<%= sessionId %>';
            const checkReady = setInterval(() => {
                fetch(`/question/${sessionId}`)
                    .then(response => {
                        if (response.ok) {
                            clearInterval(checkReady);
                            window.location.href = `/question/${sessionId}`;
                        }
                    })
                    .catch(() => {});
            }, 1000);
        </script>
    </div>
</body>
</html>